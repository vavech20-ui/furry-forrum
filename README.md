#Пишите в ЛС мне по вопросам
качаем папку , кидаем на рабочий стол, потом открываем cmd от имени администратора. 
Вводим в cmd cd C:\Users\имя юзера с которого ты сидишь\Desktop\furry-forrum
пишем npm install
npm run dev
нам выдает локал хост его в браузер вбиваем и проверяем
потом купим хост, либо на пк хостить будем
логично что придется скачать node.js с кайфом. Если чето не робит пишите мне

# Документация

В папке `docs/`:

- `docs/BACKEND_ROADMAP_OPTIONS.md` — roadmap по бэкенду, варианты и структура задач.
- `docs/PROJECT_STATUS.md` — что уже сделано и что допиливать (актуальный срез по коду).
- `docs/API_FOR_FRONTEND.MD` — описание HTTP API для фронта на этапе разработки (URL, JWT, доски/треды/посты).

